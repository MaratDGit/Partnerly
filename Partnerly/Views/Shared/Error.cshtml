@model Partnerly.Models.ViewModels.ErrorViewModel
@{
    ViewData["Title"] = "Error";
    Layout = "_BlankLayout";
}

<div class="container-xxl">
  <div class="authentication-wrapper authentication-basic container-p-y">
    <div class="authentication-inner">
      <div class="card px-sm-6 px-0">
        <div class="card-body">
          <h2 class="text-danger">Ошибка.</h2>
            @if (Model.ShowRequestId)
            {
                <p>
                    <strong>Request ID:</strong> <code>@Model.RequestId</code>
                </p>
            }

            <h3>@(Model.ErrorCode ?? "")</h3>
            <p>
                У вас нет прав для просмотра этой страницы. Если вы считаете, что это ошибка — обратитесь к администратору.
            </p>
            <p>
                <strong>@(Model.ErrorMessage)</strong>
            </p>
        </div>
      </div>
    </div>
  </div>
</div>


